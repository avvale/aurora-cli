import { Injectable } from '@nestjs/common';
import { ICommand, ofType, Saga } from '@nestjs/cqrs';
import { Observable } from 'rxjs';
import { delay, map } from 'rxjs/operators';
import { Created{{ schema.moduleName | pascalCase }}Event } from './../events/created-{{ schema.moduleName | kebabCase }}.event';

@Injectable()
export class {{ schema.moduleName | pascalCase }}Sagas
{
    /* @Saga()
    dragonKilled = (events$: Observable<any>): Observable<ICommand> =>
    {
        return events$
            .pipe(
                ofType(Created{{ schema.moduleName | pascalCase }}Event),
                delay(1000),
                map(event => {
                    console.log('Inside [HeroesGameSagas] Saga');
                    return 'command';
                }),
            );
    } */
}
