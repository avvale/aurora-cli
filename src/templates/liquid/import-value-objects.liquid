import {
    {%- for valueObject in schema.properties.valueObjects) %}
    {% assign isI18nAllowed = valueObject | allowI18nProperty: schema.moduleName %}
    {%- if valueObject.isI18n %}
    {{ schema.moduleName | pascalCase }}I18N{{ valueObject.name | pascalCase }},
    {%- else %}
    {{ schema.moduleName | pascalCase }}{{ valueObject.name | pascalCase }},
    {%- endif %}
    {%- endfor %}
} from './value-objects';